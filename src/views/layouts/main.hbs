<!DOCTYPE html>
<html lang="es">
{{>head}}
<body>
    {{>nav}}
    {{>header}}
    <main class="container">
        {{#if response}}
            {{>message}}
        {{/if}}
        {{{body}}}
    </main>
    {{>footer}}
    {{>scripts}}
</body>
</html>