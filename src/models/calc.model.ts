import { suma, resta, producto, division } from "../calc/index.ts"

export const calcOp = {
    suma, adicion: suma,
    resta, sustraccion: resta,
    producto, multiplicacion: producto,
    division, fraccion: division,
}